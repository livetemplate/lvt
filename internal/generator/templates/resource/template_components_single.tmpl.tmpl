{{/* Single-page app component - no full HTML wrapper */}}
{{define "content"}}
  {{if .DeleteConfirm}}{{template "lvt:modal:confirm:v1" .DeleteConfirm}}{{end}}
  {{if .Toasts}}{{template "lvt:toast:container:v1" .Toasts}}{{end}}
  <div>
    <!-- Add Form -->
    {{template "formBox" .}}

    <!-- Statistics -->
    {{template "stats" .}}

    <!-- Search -->
    {{template "searchBox" .}}

    <!-- Sort -->
    {{template "sortBox" .}}

    <!-- Table -->
    {{template "tableBox" .}}

    <!-- Footer -->
    <footer[[if ne (boxClass .CSSFramework) ""]] class="[[boxClass .CSSFramework]]"[[end]]>
      <p><small>Last updated: {{.LastUpdated}}</small></p>
    </footer>
  </div>
{{end}}

{{define "formContent"}}
  {{template "addForm" .}}
{{end}}

{{define "tableContent"}}
  {{template "resourceTable" .}}
  {{template "pagination" .}}
{{end}}
