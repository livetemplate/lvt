version: "2"
sql:
  - engine: "sqlite"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "models"
        out: "models"
        emit_json_tags: true
        emit_interface: false
        emit_exact_table_names: false
